HY_ID=
HY_PASSWORD=

# Optional. Leave empty to use Playwright's bundled Chromium after:
#   npx playwright install chromium
# Set to chrome to use installed Google Chrome instead.
PLAYWRIGHT_CHANNEL=
